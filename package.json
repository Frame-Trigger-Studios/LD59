{
    "name": "LD59",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview",
        "deploy": "tsc && vite build && butler push dist USERNAME/GAMENAME:html5"
    },
    "dependencies": {
        "lagom-engine": "2.0.0-beta.5"
    },
    "devDependencies": {
        "typescript": "^5.9.3",
        "vite": "^8.0.8"
    }
}